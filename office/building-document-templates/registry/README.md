# Template gallery

Registered templates live here as `<client>/<doc-type>/{template.<fmt>, manifest.json}`.
Created by `scripts/templatize.py build`. Set `$TEMPLATE_REGISTRY` to relocate this gallery outside the repo.
